[build]
  # 指定前端项目目录
  base = "hiphop-web"
  # 构建命令
  command = "npm ci && npm run build"
  # 构建输出目录（相对于 base 目录）
  publish = "dist"

# 单页应用路由配置
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量（可选，如果需要配置 API 地址）
[build.environment]
  NODE_VERSION = "18"
